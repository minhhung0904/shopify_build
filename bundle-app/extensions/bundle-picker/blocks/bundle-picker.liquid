{%- style -%}
  .bundle-picker-{{ block.id }} {
    --bp-bg: {{ block.settings.background_color }};
    --bp-text: {{ block.settings.text_color }};
    --bp-accent: {{ block.settings.accent_color }};
    --bp-radius: {{ block.settings.corner_radius }}px;
    padding: {{ block.settings.padding_top }}px 1.5rem {{ block.settings.padding_bottom }}px;
    background: var(--bp-bg);
    color: var(--bp-text);
  }

  .bundle-picker-{{ block.id }} .bp-wrap {
    max-width: 120rem;
    margin: 0 auto;
  }

  .bundle-picker-{{ block.id }} .bp-heading {
    text-align: center;
    font-size: 2.8rem;
    line-height: 1.2;
    margin: 0 0 3rem;
  }

  .bundle-picker-{{ block.id }} .bp-grid {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 2rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  @media screen and (min-width: 600px) {
    .bundle-picker-{{ block.id }} .bp-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media screen and (min-width: 990px) {
    .bundle-picker-{{ block.id }} .bp-grid {
      grid-template-columns: repeat({{ block.settings.columns_desktop }}, 1fr);
    }
  }

  .bundle-picker-{{ block.id }} .bp-card {
    position: relative;
    display: flex;
    flex-direction: column;
    background: #ffffff;
    color: #1a1a1a;
    border-radius: var(--bp-radius);
    box-shadow: 0 0.6rem 1.5rem rgba(0, 0, 0, 0.1);
    overflow: hidden;
  }

  .bundle-picker-{{ block.id }} .bp-card__badge {
    position: absolute;
    top: 1.2rem;
    left: 1.2rem;
    z-index: 1;
    padding: 0.4rem 1rem;
    border-radius: 0.6rem;
    background: var(--bp-accent);
    color: #ffffff;
    font-size: 1.2rem;
    font-weight: 700;
  }

  .bundle-picker-{{ block.id }} .bp-card__image {
    aspect-ratio: 1 / 1;
    background: #f3f3f3;
  }

  .bundle-picker-{{ block.id }} .bp-card__image img,
  .bundle-picker-{{ block.id }} .bp-card__image svg {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .bundle-picker-{{ block.id }} .bp-card__body {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    flex-grow: 1;
  }

  .bundle-picker-{{ block.id }} .bp-card__title {
    margin: 0;
    font-size: 1.9rem;
  }

  .bundle-picker-{{ block.id }} .bp-card__includes {
    margin: 0;
    font-size: 1.3rem;
    opacity: 0.7;
  }

  .bundle-picker-{{ block.id }} .bp-card__price {
    font-size: 1.8rem;
    font-weight: 700;
    margin: 0;
  }

  .bundle-picker-{{ block.id }} .bp-card__form {
    margin-top: auto;
  }

  .bundle-picker-{{ block.id }} .bp-card__submit {
    width: 100%;
    padding: 1.2rem;
    border: none;
    border-radius: 0.6rem;
    background: var(--bp-accent);
    color: #ffffff;
    font-size: 1.5rem;
    font-weight: 700;
    cursor: pointer;
  }

  .bundle-picker-{{ block.id }} .bp-card__submit:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .bundle-picker-{{ block.id }} .bp-card__error {
    color: #c31d1d;
    font-size: 1.3rem;
    margin: 0.6rem 0 0;
    display: none;
  }

  .bundle-picker-{{ block.id }} .bp-card__error.is-visible {
    display: block;
  }

  .bundle-picker-{{ block.id }} .bp-card__variant-select {
    width: 100%;
    padding: 0.6rem;
    border-radius: 0.4rem;
    border: 0.1rem solid #d9d9d9;
    font-size: 1.3rem;
  }

  .bundle-picker-{{ block.id }} .bp-card__counter {
    font-size: 1.3rem;
    opacity: 0.7;
    margin: 0;
  }

  .bundle-picker-{{ block.id }} .bp-pool {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(9rem, 1fr));
    gap: 1rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .bundle-picker-{{ block.id }} .bp-pool-item {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    padding: 0.8rem;
    border: 0.2rem solid #e3e3e3;
    border-radius: 0.6rem;
    background: none;
    cursor: pointer;
    text-align: left;
    font: inherit;
    color: inherit;
  }

  .bundle-picker-{{ block.id }} .bp-pool-item.is-selected {
    border-color: var(--bp-accent);
  }

  .bundle-picker-{{ block.id }} .bp-pool-item img {
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    border-radius: 0.4rem;
  }

  .bundle-picker-{{ block.id }} .bp-pool-item__title {
    font-size: 1.3rem;
  }

  .bundle-picker-{{ block.id }} .bp-pool-item__price {
    font-size: 1.2rem;
    opacity: 0.7;
  }

  .bundle-picker-{{ block.id }} .bp-qty {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .bundle-picker-{{ block.id }} .bp-qty__btn {
    width: 3.2rem;
    height: 3.2rem;
    border-radius: 0.4rem;
    border: 0.1rem solid #d9d9d9;
    background: #fff;
    font-size: 1.6rem;
    cursor: pointer;
  }

  .bundle-picker-{{ block.id }} .bp-qty__input {
    width: 5rem;
    text-align: center;
    padding: 0.6rem;
    border-radius: 0.4rem;
    border: 0.1rem solid #d9d9d9;
    font-size: 1.4rem;
  }

  .bundle-picker-{{ block.id }} .bp-tiers {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .bundle-picker-{{ block.id }} .bp-tier {
    display: flex;
    justify-content: space-between;
    padding: 0.6rem 0.8rem;
    border-radius: 0.4rem;
    background: #f6f6f6;
    font-size: 1.3rem;
  }

  .bundle-picker-{{ block.id }} .bp-tier.is-active {
    background: var(--bp-accent);
    color: #ffffff;
    font-weight: 700;
  }

  /* ============================================================
     Modern styling — later rules override the base above so every
     bundle type gets the upgraded look.
     ============================================================ */
  .bundle-picker-{{ block.id }} {
    --bp-accent-soft: color-mix(in srgb, var(--bp-accent) 12%, #ffffff);
    --bp-accent-ring: color-mix(in srgb, var(--bp-accent) 40%, #ffffff);
    --bp-border: #e6e6e6;
    --bp-muted: #6b7280;
  }

  .bundle-picker-{{ block.id }} .bp-heading {
    font-size: clamp(2.2rem, 3vw, 3.2rem);
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .bundle-picker-{{ block.id }} .bp-card {
    border: 1px solid var(--bp-border);
    box-shadow: 0 1px 2px rgba(16, 24, 40, 0.04), 0 8px 24px rgba(16, 24, 40, 0.06);
    transition: box-shadow 0.2s ease, transform 0.2s ease;
  }
  .bundle-picker-{{ block.id }} .bp-card:hover {
    box-shadow: 0 2px 4px rgba(16, 24, 40, 0.06), 0 18px 42px rgba(16, 24, 40, 0.1);
    transform: translateY(-2px);
  }

  .bundle-picker-{{ block.id }} .bp-card__badge {
    background: var(--bp-accent);
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }

  .bundle-picker-{{ block.id }} .bp-card__title {
    font-weight: 700;
    letter-spacing: -0.01em;
  }

  .bundle-picker-{{ block.id }} .bp-card__submit {
    border-radius: 999px;
    padding: 1.3rem 1.6rem;
    letter-spacing: 0.01em;
    transition: filter 0.15s ease, transform 0.05s ease;
  }
  .bundle-picker-{{ block.id }} .bp-card__submit:hover:not(:disabled) {
    filter: brightness(0.94);
  }
  .bundle-picker-{{ block.id }} .bp-card__submit:active:not(:disabled) {
    transform: translateY(1px);
  }

  /* mix & match / variant pool items */
  .bundle-picker-{{ block.id }} .bp-pool-item {
    border-width: 1.5px;
    border-color: var(--bp-border);
    border-radius: 1rem;
    transition: border-color 0.15s ease, box-shadow 0.15s ease, transform 0.1s ease;
  }
  .bundle-picker-{{ block.id }} .bp-pool-item:hover:not(:disabled) {
    border-color: var(--bp-accent-ring);
    transform: translateY(-1px);
  }
  .bundle-picker-{{ block.id }} .bp-pool-item.is-selected {
    border-color: var(--bp-accent);
    box-shadow: 0 0 0 3px var(--bp-accent-soft);
  }

  .bundle-picker-{{ block.id }} .bp-tier {
    border-radius: 0.8rem;
  }

  /* ---------- Tiered (Bundle & Save) ---------- */
  .bundle-picker-{{ block.id }} .bp-tierlist {
    list-style: none;
    margin: 0 0 1.4rem;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .bundle-picker-{{ block.id }} .bp-tierrow {
    position: relative;
    display: flex;
    align-items: center;
    gap: 1.2rem;
    padding: 1.4rem 1.6rem;
    border: 1.5px solid var(--bp-border);
    border-radius: 1.2rem;
    cursor: pointer;
    background: #ffffff;
    transition: border-color 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
  }
  .bundle-picker-{{ block.id }} .bp-tierrow:hover {
    border-color: var(--bp-accent-ring);
  }
  .bundle-picker-{{ block.id }} .bp-tierrow.is-selected {
    border-color: var(--bp-accent);
    box-shadow: 0 0 0 3px var(--bp-accent-soft);
    background: var(--bp-accent-soft);
  }
  .bundle-picker-{{ block.id }} .bp-tierrow__radio {
    width: 2rem;
    height: 2rem;
    accent-color: var(--bp-accent);
    flex: 0 0 auto;
  }
  .bundle-picker-{{ block.id }} .bp-tierrow__main {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    flex: 1 1 auto;
  }
  .bundle-picker-{{ block.id }} .bp-tierrow__title {
    font-size: 1.6rem;
    font-weight: 700;
  }
  .bundle-picker-{{ block.id }} .bp-tierrow__meta {
    font-size: 1.3rem;
    color: var(--bp-muted);
  }
  .bundle-picker-{{ block.id }} .bp-tierrow__save {
    font-size: 1.4rem;
    font-weight: 800;
    color: #ffffff;
    background: var(--bp-accent);
    padding: 0.4rem 1rem;
    border-radius: 999px;
    white-space: nowrap;
  }
  .bundle-picker-{{ block.id }} .bp-tierrow__ribbon {
    position: absolute;
    top: -1rem;
    right: 1.4rem;
    font-size: 1.05rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: #ffffff;
    background: var(--bp-accent);
    padding: 0.3rem 0.9rem;
    border-radius: 999px;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
  }

  /* per-slot variant swatches */
  .bundle-picker-{{ block.id }} .bp-slots {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
    margin-bottom: 1.4rem;
  }
  .bundle-picker-{{ block.id }} .bp-slot {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }
  .bundle-picker-{{ block.id }} .bp-slot__label {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--bp-muted);
  }
  .bundle-picker-{{ block.id }} .bp-swatches {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
  }
  .bundle-picker-{{ block.id }} .bp-swatch {
    width: 5.2rem;
    height: 5.2rem;
    padding: 0;
    border: 2px solid var(--bp-border);
    border-radius: 1rem;
    overflow: hidden;
    background: #f6f6f6;
    cursor: pointer;
    font-size: 1.05rem;
    line-height: 1.1;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    transition: border-color 0.15s ease, box-shadow 0.15s ease, transform 0.1s ease;
  }
  .bundle-picker-{{ block.id }} .bp-swatch:hover:not(:disabled) {
    border-color: var(--bp-accent-ring);
    transform: translateY(-1px);
  }
  .bundle-picker-{{ block.id }} .bp-swatch.is-selected {
    border-color: var(--bp-accent);
    box-shadow: 0 0 0 3px var(--bp-accent-soft);
  }
  .bundle-picker-{{ block.id }} .bp-swatch:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }
  .bundle-picker-{{ block.id }} .bp-swatch img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* big price + strikethrough compare */
  .bundle-picker-{{ block.id }} .bp-priceblock {
    display: flex;
    align-items: baseline;
    gap: 1rem;
    margin-bottom: 1.2rem;
  }
  .bundle-picker-{{ block.id }} .bp-priceblock__now {
    font-size: 2.4rem;
    font-weight: 800;
    letter-spacing: -0.02em;
  }
  .bundle-picker-{{ block.id }} .bp-priceblock__was {
    font-size: 1.6rem;
    color: var(--bp-muted);
  }
{%- endstyle -%}

<div class="bundle-picker-{{ block.id }}" {{ block.shopify_attributes }}>
  <div class="bp-wrap">
    {%- if block.settings.heading != blank -%}
      <h2 class="bp-heading">{{ block.settings.heading }}</h2>
    {%- endif -%}

    <ul class="bp-grid" role="list">
      {%- comment -%} Bundles managed centrally via the app's "Bundle" metaobject {%- endcomment -%}
      {%- if block.settings.show_metaobject_bundles -%}
        {%- for bundle in shop.metaobjects["$app:bundle"].values -%}
          {%- if bundle.status.value == "active" -%}
            {%- render 'bundle-card',
              block_id: block.id,
              handle: bundle.handle,
              title: bundle.title.value,
              badge_text: bundle.badge_text.value,
              description: bundle.description.value,
              image: bundle.image.value,
              products: bundle.products.value,
              reward_products: bundle.reward_products.value,
              bundle_type: bundle.bundle_type.value,
              discount_type: bundle.discount_type.value,
              discount_value: bundle.discount_value.value,
              volume_tiers: bundle.volume_tiers.value,
              min_items: bundle.min_items.value,
              max_items: bundle.max_items.value,
              config: bundle.config.value
            -%}
          {%- endif -%}
        {%- endfor -%}
      {%- endif -%}
    </ul>
  </div>
</div>

<script src="{{ 'bundle-picker.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "Bundle picker",
  "target": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "default": "Build your bundle",
      "label": "Heading"
    },
    {
      "type": "checkbox",
      "id": "show_metaobject_bundles",
      "label": "Also show bundles managed in the app",
      "default": true
    },
    {
      "type": "select",
      "id": "columns_desktop",
      "options": [
        { "value": "2", "label": "2" },
        { "value": "3", "label": "3" },
        { "value": "4", "label": "4" }
      ],
      "default": "3",
      "label": "Columns on desktop"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Button / accent color",
      "default": "#f58220"
    },
    {
      "type": "range",
      "id": "corner_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Card corner radius",
      "default": 12
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 60
    }
  ]
}
{% endschema %}
